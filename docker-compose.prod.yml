# Mr.Bio production stack — single VPS, behind host nginx + Cloudflare
# Run: docker compose -f docker-compose.prod.yml up -d --build
#
# Host nginx terminates TLS (Cloudflare Origin Cert) and proxies:
#   mrbionp.com      -> 127.0.0.1:8080  (frontend container)
#   api.mrbionp.com  -> 127.0.0.1:8081  (gateway container)
# All other services stay internal-only on the docker network.

x-backend-defaults: &backendDefaults
  init: true
  restart: unless-stopped
  env_file:
    - ./mr-bio-service-mr-bio-setup/.env
  networks:
    - mrbio

networks:
  mrbio:

volumes:
  redis_data:
  kafka_data:
  product_assets:

services:
  frontend:
    container_name: mrbio-frontend
    restart: unless-stopped
    build:
      context: ./Ecommerce-Frontend-Mr.Bio
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8080:80"
    networks:
      - mrbio

  redis:
    image: redis:latest
    container_name: mrbio-redis
    restart: unless-stopped
    env_file:
      - ./mr-bio-service-mr-bio-setup/.env
    volumes:
      - redis_data:/data
    networks:
      - mrbio
    command: sh -c "redis-server --requirepass $$REDIS_PASSWORD"

  kafka:
    image: apache/kafka:3.7.1
    container_name: mrbio-kafka
    restart: unless-stopped
    environment:
      - KAFKA_NODE_ID=1
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_LISTENERS=PLAINTEXT://kafka:9092,CONTROLLER://localhost:2181
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@localhost:2181
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1
      - CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk
    volumes:
      - kafka_data:/var/lib/kafka/data
    networks:
      - mrbio

  gateway:
    <<: *backendDefaults
    container_name: mrbio-gateway
    build:
      context: ./mr-bio-service-mr-bio-setup
      dockerfile: Dockerfile
      args:
        APP: gateway
    command: node apps/gateway/dist/main.js
    ports:
      - "127.0.0.1:8081:3000"
    depends_on:
      - user-auth
      - communication
      - activity-log
      - product

  user-auth:
    <<: *backendDefaults
    container_name: mrbio-user-auth
    build:
      context: ./mr-bio-service-mr-bio-setup
      dockerfile: Dockerfile
      args:
        APP: user-auth
    env_file:
      - ./mr-bio-service-mr-bio-setup/.env
      - ./mr-bio-service-mr-bio-setup/apps/user-auth/.env
    command: node apps/user-auth/dist/main.js

  communication:
    <<: *backendDefaults
    container_name: mrbio-communication
    build:
      context: ./mr-bio-service-mr-bio-setup
      dockerfile: Dockerfile
      args:
        APP: communication
    env_file:
      - ./mr-bio-service-mr-bio-setup/.env
      - ./mr-bio-service-mr-bio-setup/apps/communication/.env
    command: node apps/communication/dist/main.js
    depends_on:
      - kafka
      - redis

  activity-log:
    <<: *backendDefaults
    container_name: mrbio-activity-log
    build:
      context: ./mr-bio-service-mr-bio-setup
      dockerfile: Dockerfile
      args:
        APP: activity-log
    env_file:
      - ./mr-bio-service-mr-bio-setup/.env
      - ./mr-bio-service-mr-bio-setup/apps/activity-log/.env
    command: node apps/activity-log/dist/main.js
    depends_on:
      - kafka

  product:
    <<: *backendDefaults
    container_name: mrbio-product
    build:
      context: ./mr-bio-service-mr-bio-setup
      dockerfile: Dockerfile
      args:
        APP: product
    env_file:
      - ./mr-bio-service-mr-bio-setup/.env
      - ./mr-bio-service-mr-bio-setup/apps/product/.env
    command: node apps/product/dist/main.js
    volumes:
      - product_assets:/app/apps/product/assets

# mr-bio
to build the project, run the following command:
```bash
npm run build
```

to run the project, run the following command:
```bash
npm run start
npx turbo server --filter=product-service

to build local we should use localhost:<post>
to build through docker we should use <user-module>:<post> in env file
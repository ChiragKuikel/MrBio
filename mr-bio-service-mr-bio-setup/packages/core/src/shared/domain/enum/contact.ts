export enum PhoneType {
  CELL = 'cell',
  HOME = 'home',
  WORK = 'work',
}

export enum EmailType {
  PRIMARY = 'primary',
  WORK = 'work',
}

export enum ContactType {
  SUPPORT = 'support',
  BILLING = 'billing',
  DEVELOPER = 'developer',
}

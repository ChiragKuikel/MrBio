# Mr.Bio reverse proxy — auto TLS via Let's Encrypt
# Replace the email below with the ops contact.

{
    email ops@mrbionepal.com
}

mrbionepal.com, www.mrbionepal.com {
    encode gzip
    reverse_proxy frontend:80
}

api.mrbionepal.com {
    encode gzip
    reverse_proxy gateway:3000
}
